$(document).ready(function () {
    // array for topics
    var topics = ["", "", ""]

    // for loop function to make buttons for each item in this array
    // set attributes for buttons to make on click animation easier



    // Call Button Function Here

    // queryUrl defined here
    // call the API to display 10 static gifs
    // Animate on click using still/animate
    // Display Rating of every gif but pulling response.rating












})